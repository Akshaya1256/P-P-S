#include<stdio.h>
#define MAX 20
void main()
{
	int A[MAX][MAX],rows,cols,i,j;TRANSPOSE[i][j]
	printf("enter rows,cols");
	scanf("%d%d",&rows,&cols);
	printf("enter %d elements",rows*cols);
	for(i=0;i<rows;i++)
	{
		for(j=0;j<cols;j++)
		{
			scanf("%d",&A[i][j]);
		        printf("%5d",A[i][j]);
		}
		printf("\n");
	}
	for(i=0;i<rows;i++)
	{
		for(j=0;j<cols;j++)
		{
			TRANSPOSE[i][j]:

}

